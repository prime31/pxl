[package]
name = "pxl-dev"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    "crates/miniquad",
    "crates/pxl",
]

[dependencies]
pxl = { path = "crates/pxl" }


[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
